// import { GET_DATA, UPDATE_DATA } from '../actions';

const initialState = {
    smurfs: [],
};

export const smurfReducer = (state = initialState, action) => {
    switch(action.type) {
        default: 
            return {
                ...state
            }
    }
}